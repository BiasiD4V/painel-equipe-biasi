{
  "name": "painel-equipe-biasi",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Painel de gestão da equipe Comercial — Biasi Engenharia",
  "scripts": {
    "dev": "vercel dev",
    "migrate": "node --env-file=.env.local scripts/migrate.mjs",
    "seed": "node --env-file=.env.local scripts/seed.mjs"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "bcryptjs": "^2.4.3",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "dotenv": "^17.4.2"
  }
}
